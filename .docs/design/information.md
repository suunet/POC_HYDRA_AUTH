# information — 情報定義

> コンテキストは状態定義（states.md）と名前を揃える。属性・関連情報は「、」区切り。管理方式（DB/Redis/ステートレス）は説明列に記す。

| ID | コンテキスト | 情報 | 属性 | 関連情報 | 状態モデル | バリエーション | 説明 |
|---|---|---|---|---|---|---|---|
| INF-01 | アカウント | ユーザー情報 | メールアドレス、パスワード（bcryptハッシュ）、アカウント状態、GDPR対応カラム | INF-02（ロール情報） | STM-01（アカウント状態） | VAR-01, VAR-02 | メールアドレス、パスワード（ハッシュ）等の識別情報。管理方式: DB |
| INF-02 | アカウント | ロール情報 | ロール名 | INF-01（ユーザー情報） | — | VAR-08, VAR-09 | ユーザーに紐付くロール。1ユーザーに複数ロール付与可能（例: 一般ユーザー登録後に管理者招待を受けた場合）。管理方式: DB |
| INF-03 | セッション | アクセストークン | JWT（RS256）、ロールClaims、有効期限 | INF-04（リフレッシュトークン） | STM-02（セッション状態） | VAR-03 | JWT。短命、ステートレスに検証可能。管理方式: ステートレス |
| INF-04 | セッション | リフレッシュトークン | トークン値、有効期限、使用済みフラグ、失効理由コード | INF-01（ユーザー情報）、INF-03（アクセストークン） | STM-02（セッション状態） | VAR-04, VAR-10 | アクセストークン再発行用。長命。ローテーション運用し、再利用検知時は全セッション無効化する。管理方式: DB |
| INF-05 | ワンタイムトークン | パスワードリセットトークン | トークン値、有効期限、使用済みフラグ | INF-01（ユーザー情報） | STM-03（パスワードリセット状態） | VAR-05 | パスワードリセット用のワンタイムトークン。使い切り。管理方式: DB |
| INF-06 | ワンタイムトークン | メール確認トークン | トークンハッシュ、有効期限、使用時刻 | INF-01（ユーザー情報） | — | VAR-06 | メールアドレス確認用のワンタイムトークン。使い切り。**使用時刻（used_at）は消費だけでなく再送信時の無効化にも用いる**（CND-10・UC-004。「有効トークンは常に最大1本」）。**トークンは平文保存せずハッシュで保存・照合する。使用済みは使用時刻（使用前はnull）で表す**（漏洩対策。実装T-005で先行・カタログはP2再突合で反映）。管理方式: DB |
| INF-07 | ワンタイムトークン | 招待トークン | トークン値、有効期限、使用済みフラグ、付与ロール | INF-01（ユーザー情報）、INF-02（ロール情報） | — | VAR-07 | 管理者招待用のワンタイムトークン。使い切り。付与するロールはトークンではなくDBレコードに紐付けて管理する。管理方式: DB |
| INF-08 | レート制限 | ログイン失敗カウント | メールアドレス、失敗回数、TTL | INF-01（ユーザー情報） | — | — | メールアドレスごとの失敗回数。TTL 15分。管理方式: Redis（EXT-02） |
| INF-09 | レート制限 | ロックアウト状態 | メールアドレス、ロック状態、TTL | INF-08（ログイン失敗カウント） | — | VAR-11 | ログイン失敗10回到達後のロック状態。TTL 15分。管理方式: Redis（EXT-02） |
| INF-10 | レート制限 | パスワードリセット送信記録 | メールアドレス、最終送信時刻、TTL | INF-05（パスワードリセットトークン） | — | VAR-12 | 同一メールアドレスへの最終送信時刻。TTL 5分。管理方式: Redis（EXT-02） |
| INF-11 | レート制限 | メール確認トークン再送信記録 | メールアドレス、最終リクエスト時刻、TTL | INF-06（メール確認トークン） | — | VAR-13 | 同一メールアドレスへの再送信リクエストの最終時刻。**チェック時点で記録が確定**（送信成否に依らず試行を数える・VAR-13①）。TTL 5分。管理方式: Redis（EXT-02） |
| INF-12 | レート制限 | 招待トークン送信記録 | メールアドレス、最終送信時刻、TTL | INF-07（招待トークン） | — | VAR-14 | 同一メールアドレスへの最終送信時刻。TTL 5分。管理方式: Redis（EXT-02） |
| INF-13 | レート制限 | 登録送信記録 | メールアドレス、最終リクエスト時刻、TTL | INF-01（ユーザー情報） | — | VAR-16 | 同一メールアドレスへの登録リクエストの最終時刻。TTL 5分。登録済み/未登録に関わらず記録する（VAR-16）。管理方式: Redis（EXT-02） |
| INF-14 | レート制限 | メール確認試行記録 | IPアドレス（HMACハッシュ）、試行回数、TTL | INF-06（メール確認トークン） | — | VAR-17 | 送信元IP単位のメール確認（UC-003）試行記録。TTL 1分。キーのIPは**HMAC-SHA256＋サーバ秘密鍵でハッシュ化して保存**する（IPは個人データ。plain SHA-256はIPv4総当たりで逆引き可能なため不可）。サーバ秘密鍵が公知の開発用既定鍵と一致する場合は起動時にWARNINGで検知する（本番での既定鍵使用を防ぐ実装ガード。専用NFRは設けず本項の秘匿要求の一部として扱う）。管理方式: Redis（EXT-02） |
